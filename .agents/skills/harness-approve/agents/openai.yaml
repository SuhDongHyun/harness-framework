interface:
  display_name: "Harness Approve"
  short_description: "Approve and execute a reviewed harness run"
  default_prompt: "Use $harness-approve <run-id> to approve that draft run, execute it, and review the evidence."
policy:
  allow_implicit_invocation: false
